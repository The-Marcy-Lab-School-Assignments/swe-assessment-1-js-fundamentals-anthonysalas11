# Mod 1 Assessment - Short Response Section

Write your responses directly in this file. Follow markdown formatting guidelines.

---

## Section 1: Short Response

### Question 1


I think that this would give a syntax error.Instead of the using a console.log() we should use a return.next we use console.log() in the global space and put "react" inside the console.log() which should give us 'Everything is just fine'

### Question 2
let bestPlayer = { name: "Lebron James" };
let theGOAT = bestPlayer;
bestPlayer.name = "Michael Jordan";
console.log(theGOAT.name);

The code will log "michael jordan" into the bestPlayer object.bestPlayer which holds the object {name: "lebron james"} is stored into theGoat variable.essentially they are the same thing.So when we used the .name on bestPlayer it updated from lebron james to michael jordan.When we console.log(theGoat) still gives us michael jordan because bestPlayer in in theGoat variable.



### Question 3
const theHustler = 'Laisha';

const shoutOut = () => {
  const theHustler = 'Paul';
  console.log(`${theHustler} is the hardest working person in the room.`);
}

shoutOut();
console.log(`${theHustler} is also the hardest working person in the room.`);

The 

### Question 4


### Question 5


### Question 6


### Question 7


### Question 8

